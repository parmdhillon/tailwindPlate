{
  "name": "my-tailwind-boilerplate",
  "version": "1.0.0",
  "description": "Simple Tailwind CSS Boilerplate",
  "main": "",
  "scripts": {
    "dev:css": "npx tailwindcss -i ./global.css -o ./public/style/style.css --watch --jit",
    "build:css": "set NODE_ENV=production && npx tailwindcss -i ./global.css -o ./public/style/style.css --jit --minify"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.3.6",
    "postcss": "^8.3.8",
    "tailwindcss": "^2.2.16"
  }
}
